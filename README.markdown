Customized Vim's start screen.


It uses the file Dashboard.txt on the nvim folder.

Set `g:Startscreen_function` to your prefered function to run on startup.

See `:help startscreen` for the full documentation.
